// Date: July 26, 2024
// Problem# C
// Status: Doesn't Work :(

// Start...
/*
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;


void skipLine(std::istream& cin) {
	// Skip characters until the end of the current line
	cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');
}
int main()
{

	int t;
	cin >> t;

	while (t--)
	{
		int n, q;
		cin >> n >> q;
		const int size = n;
		string line1, line2;
		skipLine(cin);
		cin >> line1;
		skipLine(cin);
		cin >> line2;
		skipLine(cin);
		int cols = 2;
		vector<vector<int>> query(q, vector<int>(cols));


	}
}
*/